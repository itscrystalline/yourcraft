{
    "version": "0.2.0",
    "configurations": [
        {
            "type": "codelldb",
            "request": "launch",
            "name": "Debug Server (flat)",
            "cargo": {
                "args": [
                    "build"
                ]
            },
            "args": ["flat"],
            "program": "target/debug/yourcraft-server",
            "cwd": "${workspaceFolder}",
            "preLaunchTask": "Rust: Build"
        } 
    ]
}
